[pytest]
testpaths = tests
addopts = -q --no-header -rfs
markers =
    live_scoring: needs a running scoring service at SCORING_URL (auto-skipped when absent)
filterwarnings =
    ignore::DeprecationWarning
