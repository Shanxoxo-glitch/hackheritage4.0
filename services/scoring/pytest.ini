[pytest]
testpaths = tests
addopts = -q --no-header -rfs
markers =
    integration: loads the real local models and a real RAVDESS clip (auto-skipped when weights are absent)
filterwarnings =
    ignore::DeprecationWarning
